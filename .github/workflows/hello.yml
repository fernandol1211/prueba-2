name: Prueba de Hello World

on:
  push:
    branches:
      - develop

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "heeeeello fernando"
